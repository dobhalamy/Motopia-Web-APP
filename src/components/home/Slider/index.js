/* eslint-disable react/prop-types */
import React from 'react';
import SliderComponent from './SliderComponent';

const Slider = ({ heroImages }) => (
  <SliderComponent data={heroImages} />
);

export default Slider;
